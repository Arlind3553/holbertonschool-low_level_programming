#include "main.h"
#include <stdlib.h>
#include <stdio.h>

char *str_concat(char *s1, char *s2)
{
	char *new;
	unsigned int len1, len2, i;
	len1 = 0;
	len2 = 0;
	i = 0;

	if (s1 == NULL)
		s1 = "";
	if (s2 = NULL)
	       s2 = "";

}
