#include "main.h"
/**
 * add - adds two numbers together
 * @a: first input
 * @b: second input
 * Return: returns the sum of the numbers
 */
int add(int i, int j)
{
	int s;

	s = i + j;
	return (s);
}
